# 📋 Руководство по использованию локаторов в E2E тестах

## 🔍 Анализ текущего состояния

### Форматы локаторов в проекте

1. **OnboardingLocators.ts** - использует Android resource-id:
   ```typescript
   onboardingLogo: 'com.pears.pass:id/onboarding-logo'
   ```
   ❌ **Проблема**: Работает только на Android, не работает на iOS

2. **HomeLocators.ts** - использует accessibility ID:
   ```typescript
   homeLogoLock: '~logo-lock'
   ```
   ✅ **Правильно**: Работает на обеих платформах

3. **SidebarLocators.ts** - смешанный подход:
   - XPath: `'//android.widget.TextView[@text="Folder"]'`
   - Accessibility ID: `'~qr-code'`
   ⚠️ **Частично правильно**: XPath работает, но менее надежен

---

## ✅ Рекомендации по использованию локаторов

### Приоритет выбора локаторов (от лучшего к худшему):

#### 1. **Accessibility ID** (рекомендуется) ⭐
```typescript
// Формат: префикс ~ + accessibility ID
homeLogoLock: '~logo-lock'
onboardingLogo: '~onboarding-logo'
```
**Преимущества:**
- ✅ Работает на Android и iOS
- ✅ Стабильный и быстрый
- ✅ Не зависит от структуры UI
- ✅ Рекомендуется Appium/WebdriverIO

**Когда использовать:**
- Для всех элементов, которые должны быть доступны на обеих платформах
- Когда в приложении установлены accessibility labels/IDs

---

#### 2. **Android Resource ID** (только для Android)
```typescript
// Формат: package:id/resource-id
onboardingLogo: 'com.pears.pass:id/onboarding-logo'
```
**Преимущества:**
- ✅ Быстрый и надежный для Android
- ✅ Прямая связь с ресурсами приложения

**Недостатки:**
- ❌ Не работает на iOS
- ❌ Требует отдельные локаторы для iOS

**Когда использовать:**
- Только если тесты запускаются исключительно на Android
- Если нет accessibility ID в приложении

---

#### 3. **XPath** (использовать с осторожностью)
```typescript
// Формат: XPath выражение
sidebarTitle: '//android.widget.TextView[@text="Folder"]'
```
**Преимущества:**
- ✅ Гибкий и мощный
- ✅ Может найти элементы по тексту, атрибутам

**Недостатки:**
- ⚠️ Медленнее других методов
- ⚠️ Хрупкий (ломается при изменении структуры UI)
- ⚠️ Сложнее поддерживать

**Когда использовать:**
- Когда нет других вариантов
- Для динамических элементов
- Для сложных поисков по тексту

---

## 🎯 Правила для OnboardingLocators.ts

### Текущая проблема:
Файл использует Android resource-id формат, который не работает на iOS:
```typescript
// ❌ Неправильно (только Android)
onboardingLogo: 'com.pears.pass:id/onboarding-logo'
```

### Правильное решение:
Использовать accessibility ID для кроссплатформенности:
```typescript
// ✅ Правильно (Android + iOS)
onboardingLogo: '~onboarding-logo'
onboardingProgressBar: '~onboarding-progress-bar'
onboardingContinueButton: '~onboarding-continue-button'
```

---

## 📝 Чеклист для проверки локаторов

- [ ] Все локаторы имеют единый формат в файле
- [ ] Используется accessibility ID (`~`) для кроссплатформенных тестов
- [ ] XPath используется только когда нет альтернативы
- [ ] Локаторы уникальны и не дублируются
- [ ] Имена локаторов понятны и описательны
- [ ] Локаторы не зависят от текста, который может измениться

---

## 🔧 Как исправить OnboardingLocators.ts

### Шаг 1: Проверить, есть ли accessibility ID в приложении
- Используйте Appium Inspector или UI Automator Viewer
- Проверьте, установлены ли `accessibilityLabel` или `testID` в React Native

### Шаг 2: Заменить формат локаторов
```typescript
// Было:
onboardingLogo: 'com.pears.pass:id/onboarding-logo'

// Стало:
onboardingLogo: '~onboarding-logo'
```

### Шаг 3: Протестировать на обеих платформах
```bash
# Android
npm run e2e:android:emulator

# iOS (если доступно)
npm run e2e:ios:local
```

---

## 📚 Дополнительные ресурсы

- [WebdriverIO Selectors](https://webdriver.io/docs/selectors)
- [Appium Selectors](https://appium.io/docs/en/2.1/guides/element-finding/)
- [React Native TestID](https://reactnative.dev/docs/accessibility#testid)

---

## ⚠️ Важные замечания

1. **Единообразие**: Используйте один формат локаторов в рамках одного файла
2. **Кроссплатформенность**: Для тестов на Android и iOS используйте accessibility ID
3. **Поддержка**: XPath сложнее поддерживать, избегайте его когда возможно
4. **Производительность**: Accessibility ID быстрее XPath
