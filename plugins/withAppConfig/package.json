{
  "name": "with-app-config",
  "version": "1.0.0",
  "main": "app.plugin.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@expo/config-plugins": "~9.0.0",
    "typescript": "^5.3.0"
  }
}
